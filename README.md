# «Больше драмы» · команда

Пульт команды на день мастер-класса 26.09: что происходит сейчас, что дальше, кто за что отвечает.

План, люди и роли — в `src/plan.ts`. Задачи привязаны к ролям, поэтому, если человек
меняет роль, правится одна строка в `roles`.

Проверить любой момент дня: `?t=14:20`.

```
npm install
npm run dev
```

Публикуется на GitHub Pages при пуше в `main`.
